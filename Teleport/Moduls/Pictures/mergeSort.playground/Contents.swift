

var arrayToSort = [7, 2, 6, 3, 9]

func mergeSort(_ array: [Int]) -> [Int] {
    let middleIndex = array.count / 2
    let leftArray = array[0..<middleIndex]
    let rightArray = array[middleIndex..<array.count]
    leftArray
    middleIndex
    rightArray
    return array
}

mergeSort(arrayToSort)

let array2 = [6, 3, 9]
array2.count / 2
